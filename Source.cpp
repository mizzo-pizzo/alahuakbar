#include <iostream>
using namespace std;
int main() {
	/*
	//If9.ƒаны две переменные вещественного типа : A, B.ѕерераспределить значени€ данных переменных так, чтобы в A оказалось меньшее из значений,
	//а в B Ч большее.¬ывести новые значени€ переменных A и B.
	double a, b;
	cin >> a >> b;
	// 13 - не надо провер€ть
    // 31
	if (a > b) {
		int t = a;
		a = t;
		b = t;
	}
	cout << a << " " << b << endl; */

  /*  //If10.ƒаны две переменные целого типа : A и B.≈сли их значени€ не равны,
	//то присвоить каждой переменной сумму этих значений, а если равны,
	//то присвоить переменным нулевые значени€.¬ывести новые значени€
	//переменных A и B.
	int a, b;
	cin >> a >> b;
	if (a != b) {
		int sum = a + b;
		a = sum;
		b = sum;
		
	}
	 
	else { 
		a = 0;
		b = 0;
		// a = b = 0;
	}
	cout << a << " " << b << endl;*/

	/* //if 11
	int a, b;
	cin >> a >> b;
	if (a != b) {
		if (a > b) {
			b = a;
		}
		else {
			a = b;
		}
	}
	else {
		a = 0;
		b = 0;
	}
	cout << a << " " << b << endl; */
	
/*	//if 12.ƒаны три числа.Ќайти наименьшее из них.
	int a, b, c;
	cin >> a >> b >> c;
	if (a < b && a < c) {
		cout << a << endl;
	}
	if (b < a && b < c) {
		cout << b << endl;
	}
	else if (c < a && c < b) {
		cout << c << endl;
	} */

	//If13. ƒаны три числа. Ќайти среднее из них (то есть число, расположенное
	//между наименьшим и наибольшим).
	 
	/*int a, b, c;
	cin >> a >> b >> c;
	if (b <= a && a <= c) || (b >= a && a >= c) {
		cout << a << endl;
	}
	if (a <= b && b <= c) || (a >= b && b > c) {
		cout << b << endl;
	}
	else if (c <= a && c <= b) || (c >= a && c >= b) {
		cout << c << endl;
	} */

	/*//If14.ƒаны три числа.¬ывести вначале наименьшее, а затем наибольшее из
	//данных чисел.	int a, b, c;
	cin >> a >> b >> c;
	if (a < b && a < c) {
		cout << a << endl;
	}
	if (b < a && b < c) {
		cout << b << endl;
	}
	else if (c < a && c < b) {
		cout << c << endl;
		if (a > b && a > c) {
			cout << a << endl;

		}
		if (b > a && b > c) {
			cout << b << endl;
		}
		else if (c > a && c > b) {
			cout << c << endl;
		}
		cout << a << " " << b << c endl; */

	}